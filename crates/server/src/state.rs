use std::sync::Arc;

#[derive(Clone)]
pub struct AppState {
    // pub config: Arc<Config>,
    // pub user_repo: Arc<dyn UserRepo>,
    // pub tx_service: Arc<TransactionService>,
    // pub http_client: Arc<reqwest::Client>,
    // pub metrics: Arc<MetricsRegistry>,
    // pub clock: Arc<dyn Clock>, // for testable timestamps
}
