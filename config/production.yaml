server:
  bind_addr: "0.0.0.0:3000"
  database_url: "crates/storage-diesel/data/production.sqlite"
  database_pool_size: 16
auth:
  single_use_token_ttl_seconds: 15
  admin_session_ttl_seconds: 180
logging:
  filter: "tower_http=info,axum=info,server=info"
tui:
  api_base_url: "http://localhost:3000"
  event_poll_interval_ms: 20
  scanner_max_gap_ms: 80
  locale: "nb"
